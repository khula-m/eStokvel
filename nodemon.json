{
  "watch": ["src", "prisma", "server.ts"],
  "ext": "ts,js,json,prisma",
  "ignore": ["src/**/*.spec.ts", "dist", "node_modules"],
  "exec": "ts-node --transpile-only server.ts",
  "env": {
    "NODE_ENV": "development",
    "PORT": "5000"
  },
  "events": {
    "restart": "echo '?? Server restarted due to file changes...'",
    "crash": "echo '?? Server crashed! Waiting for changes...'"
  }
}
